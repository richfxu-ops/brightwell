---
status: To Do
size: Large
created: 2026-07-15
title: Map & routing
---

## Context

Backfilled from `QUESTIONS.md` §C4 (still-open) — the FIDELITY map that Phase 5 deliberately
deferred. Today the run is single-node: the crown is the final asking on the current town, not a
place you route to (`run-frame.md` scope boundary; D-008 fidelity ladder: "v1 simplifies the map
to 2–3 offered next towns per dusk").

**The open question (C4):** how many towns per leg of the journey, and how many card/route offers
appear each dusk?

**Canon recommendation on the table (not yet decided):** ~3 offers per dusk from a per-leg pool of
~5–6 towns, with **camp always offered** — the ⅔-RETURN rule (B1, locked) needs camping to exist as
a choice. The tiers/sizes/rings schedule and the crown floor are already [CANON] in §C; this task is
about the *shape of the map those play out on*.

Depends on nothing further in the engine (Phases 4–6 are real); it's the expansion that turns the
single node into a walked route. Pairs with the meta-layer ([[meta-progression]]) for across-run
structure.

## Scope to define (when picked up)

- Nodes per leg, offers per dusk, and how camp sits alongside route offers.
- How a town's rings/tier are drawn for each offered node.
- Interaction with the dusk sweep (B1) and the ⅔-RETURN camping rule.
- FIDELITY boundary vs. the full road-graph (delayed tellings, road-memory) — likely still deferred.
